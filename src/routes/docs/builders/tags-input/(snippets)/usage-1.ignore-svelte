<script lang="ts">
	import { createTagsInput } from '@melt-ui/svelte';
	const {
		root,
		input,
		tag,
		deleteTrigger,
		edit,
		options,
		tags,
		selected,
		inputValue,
		inputInvalid,
		isSelected,
	} = createTagsInput();
</script>
