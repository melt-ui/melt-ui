import disable from './disable.ignore-svelte?raw';
import indeterminate from './indeterminate.ignore-svelte?raw';
import controlled from './controlled.ignore-svelte?raw';

export const snippets = {
	disable,
	controlled,
	indeterminate,
};
